- page_title!('Editando area')

#region-areas.container

  .region
    .region-title
      %h1
        Editando
        %b
          area

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @area, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
