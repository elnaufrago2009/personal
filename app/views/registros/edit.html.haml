- page_title!('Editando registro')

#region-registros.container

  .region
    .region-title
      %h1
        Editando
        %b
          registro

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @registro, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
