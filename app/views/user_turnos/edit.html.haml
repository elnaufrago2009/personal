- page_title!('Editando user_turno')

#region-user_turnos.container

  .region
    .region-title
      %h1
        Editando
        %b
          user_turno

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @user_turno, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
