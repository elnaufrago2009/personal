= form_for @center_work, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @center_work.errors.any?
    #error_explanation
      %h2= "#{pluralize(@center_work.errors.count, "error")}"
      %ul
        - @center_work.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :name, class: 'col-sm-2 control-label'
    .col-sm-3
      = f.text_field :name, class: 'form-control'
  .form-group
    = f.label :departamento, class: 'col-sm-2 control-label'
    .col-sm-3
      = f.select :deparment_id, Deparment.all.collect{|x| [x.name, x.id]}, {:include_blank => 'Departamento'}, class: 'form-control'
  .form-group
    .col-sm-2
    .col-sm-4 
      = f.submit 'Guardar', class: 'btn btn-default'
