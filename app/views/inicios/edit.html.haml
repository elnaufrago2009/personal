- page_title!('Editando inicio')

#region-inicios.container

  .region
    .region-title
      %h1
        Editando
        %b
          inicio

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @inicio, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
